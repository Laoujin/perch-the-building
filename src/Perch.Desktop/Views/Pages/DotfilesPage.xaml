<Page
    x:Class="Perch.Desktop.Views.Pages.DotfilesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:vm="clr-namespace:Perch.Desktop.ViewModels"
    d:DataContext="{d:DesignInstance vm:DotfilesViewModel}"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Loaded="OnLoaded">

    <Grid Margin="24,16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <ui:TextBlock FontTypography="Title" Text="Dotfiles" />
                <ui:TextBlock
                    FontTypography="Caption"
                    Opacity="0.6"
                    Margin="0,4,0,0">
                    <ui:TextBlock.Text>
                        <MultiBinding StringFormat="{}{0} of {1} links active">
                            <Binding Path="LinkedCount" />
                            <Binding Path="TotalCount" />
                        </MultiBinding>
                    </ui:TextBlock.Text>
                </ui:TextBlock>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <ui:TextBox
                    PlaceholderText="Search modules..."
                    Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                    Width="200"
                    Margin="0,0,8,0" />
                <ui:Button
                    Icon="{ui:SymbolIcon ArrowClockwise24}"
                    Command="{Binding RefreshCommand}" />
            </StackPanel>
        </Grid>

        <!-- Loading -->
        <ui:ProgressRing
            Grid.Row="1"
            IsIndeterminate="True"
            Width="24"
            Height="24"
            Margin="0,12,0,0"
            HorizontalAlignment="Left"
            Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}" />

        <!-- No config -->
        <ui:TextBlock
            Grid.Row="2"
            FontTypography="Body"
            Text="No config repository configured. Go to Settings to set one up."
            Opacity="0.6"
            VerticalAlignment="Center"
            HorizontalAlignment="Center"
            Visibility="{Binding HasConfigRepo, Converter={StaticResource InverseBooleanToVisibilityConverter}}" />

        <!-- Module list -->
        <ListView
            Grid.Row="2"
            Margin="0,12,0,0"
            ItemsSource="{Binding FilteredModules}"
            Visibility="{Binding HasConfigRepo, Converter={StaticResource BooleanToVisibilityConverter}}"
            ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListView.ItemTemplate>
                <DataTemplate DataType="{x:Type vm:ModuleItemViewModel}">
                    <ui:CardExpander Margin="0,0,0,4">
                        <ui:CardExpander.Header>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                    <ui:TextBlock FontTypography="BodyStrong" Text="{Binding DisplayName}" />
                                    <ui:TextBlock FontTypography="Caption" Opacity="0.5" Text="{Binding Name}" />
                                </StackPanel>

                                <Border
                                    Grid.Column="1"
                                    CornerRadius="4"
                                    Padding="8,4"
                                    Margin="8,0"
                                    VerticalAlignment="Center">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="Background" Value="#22F59E0B" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsFullyLinked}" Value="True">
                                                    <Setter Property="Background" Value="#2234D399" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <ui:TextBlock FontTypography="Caption" Text="{Binding StatusSummary}">
                                        <ui:TextBlock.Style>
                                            <Style TargetType="ui:TextBlock">
                                                <Setter Property="Foreground" Value="#F59E0B" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsFullyLinked}" Value="True">
                                                        <Setter Property="Foreground" Value="#34D399" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </ui:TextBlock.Style>
                                    </ui:TextBlock>
                                </Border>

                                <ui:TextBlock
                                    Grid.Column="2"
                                    FontTypography="Caption"
                                    Opacity="0.4"
                                    VerticalAlignment="Center">
                                    <ui:TextBlock.Style>
                                        <Style TargetType="ui:TextBlock">
                                            <Setter Property="Text" Value="Enabled" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Enabled}" Value="False">
                                                    <Setter Property="Text" Value="Disabled" />
                                                    <Setter Property="Foreground" Value="#EF4444" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </ui:TextBlock.Style>
                                </ui:TextBlock>
                            </Grid>
                        </ui:CardExpander.Header>

                        <ui:CardExpander.Content>
                            <StackPanel Margin="0,4">
                                <ItemsControl ItemsSource="{Binding Links}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate DataType="{x:Type vm:LinkItemViewModel}">
                                            <Grid Margin="0,4">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto" />
                                                    <ColumnDefinition Width="*" />
                                                    <ColumnDefinition Width="Auto" />
                                                </Grid.ColumnDefinitions>

                                                <ui:SymbolIcon
                                                    Grid.Column="0"
                                                    Margin="0,0,8,0"
                                                    VerticalAlignment="Center">
                                                    <ui:SymbolIcon.Style>
                                                        <Style TargetType="ui:SymbolIcon">
                                                            <Setter Property="Symbol" Value="Link24" />
                                                            <Setter Property="Foreground" Value="#34D399" />
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding Status}" Value="{x:Static vm:LinkStatus.NotLinked}">
                                                                    <Setter Property="Symbol" Value="LinkDismiss24" />
                                                                    <Setter Property="Foreground" Value="#F59E0B" />
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding Status}" Value="{x:Static vm:LinkStatus.Drift}">
                                                                    <Setter Property="Symbol" Value="Warning24" />
                                                                    <Setter Property="Foreground" Value="#F59E0B" />
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding Status}" Value="{x:Static vm:LinkStatus.Conflict}">
                                                                    <Setter Property="Symbol" Value="ErrorCircle24" />
                                                                    <Setter Property="Foreground" Value="#EF4444" />
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </ui:SymbolIcon.Style>
                                                </ui:SymbolIcon>

                                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                                    <ui:TextBlock FontTypography="Caption" Text="{Binding Source}" />
                                                    <ui:TextBlock FontTypography="Caption" Opacity="0.5" Text="{Binding Target}" TextTrimming="CharacterEllipsis" />
                                                </StackPanel>

                                                <ui:TextBlock
                                                    Grid.Column="2"
                                                    FontTypography="Caption"
                                                    VerticalAlignment="Center"
                                                    Text="{Binding StatusDisplay}" />
                                            </Grid>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </ui:CardExpander.Content>
                    </ui:CardExpander>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>

</Page>
