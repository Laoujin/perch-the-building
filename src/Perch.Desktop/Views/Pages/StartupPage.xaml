<Page
    x:Class="Perch.Desktop.Views.Pages.StartupPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:vm="clr-namespace:Perch.Desktop.ViewModels"
    xmlns:models="clr-namespace:Perch.Desktop.Models"
    d:DataContext="{d:DesignInstance vm:StartupViewModel}"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Foreground="White"
    Loaded="OnLoaded"
    Unloaded="OnUnloaded">

    <Grid Margin="24,16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <ui:TextBlock Grid.Column="0" FontTypography="Title" Text="Startup Apps" />

            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <ui:TextBox
                    PlaceholderText="Search startup items..."
                    Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                    Width="200"
                    Margin="0,0,8,0" />
                <ui:Button
                    Icon="{ui:SymbolIcon ArrowClockwise24}"
                    Command="{Binding RefreshCommand}" />
            </StackPanel>
        </Grid>

        <!-- Loading -->
        <ui:ProgressRing
            Grid.Row="1"
            IsIndeterminate="True"
            Width="32"
            Height="32"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}" />

        <!-- Startup items -->
        <ScrollViewer
            Grid.Row="1"
            Margin="0,16,0,0"
            VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <!-- Current Startup Items -->
                <TextBlock
                    Text="Current Startup Items"
                    FontSize="14"
                    FontWeight="SemiBold"
                    Foreground="White"
                    Margin="0,0,0,12" />

                <!-- Empty state -->
                <TextBlock
                    x:Name="EmptyState"
                    Text="No startup items found"
                    FontSize="12"
                    Foreground="{StaticResource TextDimBrush}"
                    Margin="0,0,0,12"
                    Visibility="Collapsed" />

                <ItemsControl ItemsSource="{Binding FilteredItems}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate DataType="{x:Type models:StartupCardModel}">
                            <Border
                                Background="{StaticResource CardBackgroundBrush}"
                                BorderBrush="{StaticResource PanelBorderBrush}"
                                BorderThickness="1"
                                CornerRadius="8"
                                Padding="12"
                                Width="300"
                                Margin="0,0,12,12">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>

                                    <!-- Name + toggle -->
                                    <Grid Grid.Row="0">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>

                                        <TextBlock
                                            Grid.Column="0"
                                            Text="{Binding Name}"
                                            FontSize="13"
                                            FontWeight="SemiBold"
                                            Foreground="White"
                                            TextTrimming="CharacterEllipsis"
                                            ToolTip="{Binding Name}"
                                            VerticalAlignment="Center" />

                                        <ui:ToggleSwitch
                                            Grid.Column="1"
                                            IsChecked="{Binding IsEnabled, Mode=OneWay}"
                                            Checked="OnToggleChecked"
                                            Unchecked="OnToggleUnchecked" />
                                    </Grid>

                                    <!-- Command -->
                                    <TextBlock
                                        Grid.Row="1"
                                        Text="{Binding Command}"
                                        FontSize="11"
                                        Foreground="{StaticResource TextMutedBrush}"
                                        TextTrimming="CharacterEllipsis"
                                        ToolTip="{Binding Command}"
                                        Margin="0,4,0,0" />

                                    <!-- Source badge + remove button -->
                                    <Grid Grid.Row="2" Margin="0,8,0,0">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>

                                        <Border
                                            Grid.Column="0"
                                            Background="{StaticResource SourceBadgeBgBrush}"
                                            CornerRadius="4"
                                            Padding="6,2"
                                            HorizontalAlignment="Left">
                                            <TextBlock
                                                Text="{Binding SourceLabel}"
                                                FontSize="10"
                                                Foreground="{StaticResource AccentBrush}" />
                                        </Border>

                                        <ui:Button
                                            Grid.Column="1"
                                            Icon="{ui:SymbolIcon Delete24}"
                                            ToolTip="Remove from startup"
                                            Padding="4"
                                            Height="26"
                                            Click="OnRemoveClick" />
                                    </Grid>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </ScrollViewer>
    </Grid>

</Page>
