<UserControl
    x:Class="Perch.Desktop.Views.Controls.AppCard"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:controls="clr-namespace:Perch.Desktop.Views.Controls">
    <Border
        x:Name="CardBorder"
        Background="#1A1A2E"
        BorderThickness="1"
        CornerRadius="8"
        Padding="12"
        Width="280">
        <Border.Style>
            <Style TargetType="Border">
                <Setter Property="BorderBrush" Value="#2A2A3E" />
                <Style.Triggers>
                    <DataTrigger Binding="{Binding IsManaged, RelativeSource={RelativeSource AncestorType=UserControl}}" Value="True">
                        <Setter Property="BorderBrush" Value="#10B981" />
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </Border.Style>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- Row 0: Status ribbon + badges + toggle -->
            <Grid Grid.Row="0" Margin="0,0,0,6">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <controls:StatusRibbon
                    Grid.Column="0"
                    Status="{Binding Status, RelativeSource={RelativeSource AncestorType=UserControl}}"
                    VerticalAlignment="Center" />

                <!-- Suggested badge -->
                <Border
                    Grid.Column="1"
                    Background="#1E3A5F"
                    CornerRadius="4"
                    Padding="4,1"
                    Margin="4,0,0,0"
                    VerticalAlignment="Center"
                    Visibility="{Binding IsSuggested, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <TextBlock Text="Suggested" FontSize="9" Foreground="#60A5FA" />
                </Border>

                <!-- Kind badge -->
                <Border
                    Grid.Column="2"
                    Background="#2A2A3E"
                    CornerRadius="4"
                    Padding="4,1"
                    Margin="4,0,0,0"
                    VerticalAlignment="Center"
                    Visibility="{Binding KindBadge, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource CountToVisibilityConverter}}">
                    <TextBlock
                        Text="{Binding KindBadge, RelativeSource={RelativeSource AncestorType=UserControl}}"
                        FontSize="9"
                        Foreground="#888888" />
                </Border>

                <!-- Top Pick badge -->
                <Border
                    Grid.Column="3"
                    Background="#1A10B981"
                    CornerRadius="4"
                    Padding="4,1"
                    Margin="4,0,0,0"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Left"
                    Visibility="{Binding IsTopPick, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <TextBlock Text="Top Pick" FontSize="9" Foreground="#10B981" />
                </Border>

                <ui:ToggleSwitch
                    Grid.Column="4"
                    IsChecked="{Binding IsManaged, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=OneWay}"
                    IsEnabled="{Binding CanToggle, RelativeSource={RelativeSource AncestorType=UserControl}}"
                    Checked="OnToggleChanged"
                    Unchecked="OnToggleChanged"
                    VerticalAlignment="Center" />
            </Grid>

            <!-- Row 1: Logo + Name -->
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Grid Grid.Column="0" Width="24" Height="24" VerticalAlignment="Center" Margin="0,0,8,0">
                    <Border
                        x:Name="FallbackIcon"
                        Width="24"
                        Height="24"
                        CornerRadius="12"
                        Background="#2A2A3E">
                        <TextBlock
                            x:Name="FallbackInitial"
                            FontSize="12"
                            FontWeight="SemiBold"
                            Foreground="#9CA3AF"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center" />
                    </Border>
                    <Border CornerRadius="12" Width="24" Height="24">
                        <Border.Clip>
                            <EllipseGeometry Center="12,12" RadiusX="12" RadiusY="12" />
                        </Border.Clip>
                        <Image
                            x:Name="LogoImage"
                            Width="24"
                            Height="24"
                            Stretch="UniformToFill"
                            RenderOptions.BitmapScalingMode="HighQuality" />
                    </Border>
                </Grid>

                <TextBlock
                    Grid.Column="1"
                    Text="{Binding DisplayLabel, RelativeSource={RelativeSource AncestorType=UserControl}}"
                    FontSize="13"
                    FontWeight="SemiBold"
                    Foreground="White"
                    VerticalAlignment="Center"
                    TextTrimming="CharacterEllipsis" />
            </Grid>

            <!-- Row 2: Description (2-line max) -->
            <TextBlock
                Grid.Row="2"
                Text="{Binding Description, RelativeSource={RelativeSource AncestorType=UserControl}}"
                FontSize="11"
                Foreground="#888888"
                TextWrapping="Wrap"
                TextTrimming="CharacterEllipsis"
                MaxHeight="32"
                Margin="0,4,0,0"
                Visibility="{Binding Description, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource CountToVisibilityConverter}}" />

            <!-- Row 3: Links bar (Website, Docs, GitHub) -->
            <StackPanel
                Grid.Row="3"
                Orientation="Horizontal"
                Margin="0,6,0,0">
                <Button
                    Background="Transparent"
                    BorderThickness="0"
                    Padding="4,2"
                    Margin="0,0,4,0"
                    Click="OnLinkClick"
                    Tag="{Binding Website, RelativeSource={RelativeSource AncestorType=UserControl}}"
                    ToolTip="{Binding Website, RelativeSource={RelativeSource AncestorType=UserControl}}"
                    Visibility="{Binding Website, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource CountToVisibilityConverter}}">
                    <StackPanel Orientation="Horizontal">
                        <ui:SymbolIcon Symbol="Globe24" FontSize="12" Foreground="#6B7280" />
                        <TextBlock Text="Website" FontSize="10" Foreground="#6B7280" Margin="4,0,0,0" VerticalAlignment="Center" />
                    </StackPanel>
                </Button>
                <Button
                    Background="Transparent"
                    BorderThickness="0"
                    Padding="4,2"
                    Margin="0,0,4,0"
                    Click="OnLinkClick"
                    Tag="{Binding Docs, RelativeSource={RelativeSource AncestorType=UserControl}}"
                    ToolTip="{Binding Docs, RelativeSource={RelativeSource AncestorType=UserControl}}"
                    Visibility="{Binding Docs, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource CountToVisibilityConverter}}">
                    <StackPanel Orientation="Horizontal">
                        <ui:SymbolIcon Symbol="Document24" FontSize="12" Foreground="#6B7280" />
                        <TextBlock Text="Docs" FontSize="10" Foreground="#6B7280" Margin="4,0,0,0" VerticalAlignment="Center" />
                    </StackPanel>
                </Button>
                <Button
                    Background="Transparent"
                    BorderThickness="0"
                    Padding="4,2"
                    Click="OnLinkClick"
                    Tag="{Binding GitHub, RelativeSource={RelativeSource AncestorType=UserControl}}"
                    ToolTip="{Binding GitHub, RelativeSource={RelativeSource AncestorType=UserControl}}"
                    Visibility="{Binding GitHub, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource CountToVisibilityConverter}}">
                    <StackPanel Orientation="Horizontal">
                        <ui:SymbolIcon Symbol="Code24" FontSize="12" Foreground="#6B7280" />
                        <TextBlock Text="GitHub" FontSize="10" Foreground="#6B7280" Margin="4,0,0,0" VerticalAlignment="Center" />
                        <TextBlock
                            Text="{Binding GitHubStarsFormatted, RelativeSource={RelativeSource AncestorType=UserControl}}"
                            FontSize="9"
                            Foreground="#F59E0B"
                            Margin="2,0,0,0"
                            VerticalAlignment="Center"
                            Visibility="{Binding GitHubStarsFormatted, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource CountToVisibilityConverter}}" />
                    </StackPanel>
                </Button>
            </StackPanel>

            <!-- Row 4: Tag pills -->
            <ItemsControl
                Grid.Row="4"
                Margin="0,6,0,0"
                Visibility="{Binding Tags, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource CountToVisibilityConverter}}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemsSource>
                    <Binding Path="Tags" RelativeSource="{RelativeSource AncestorType=UserControl}" />
                </ItemsControl.ItemsSource>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border
                            Background="#2A2A3E"
                            CornerRadius="8"
                            Padding="6,2"
                            Margin="0,0,4,2"
                            Cursor="Hand"
                            MouseLeftButtonUp="OnTagClick">
                            <TextBlock Text="{Binding}" FontSize="10" Foreground="#9CA3AF" />
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <!-- Row 5: License badge -->
            <Border
                Grid.Row="5"
                Background="#2A2A3E"
                CornerRadius="4"
                Padding="4,1"
                Margin="0,6,0,0"
                HorizontalAlignment="Left"
                Visibility="{Binding License, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource CountToVisibilityConverter}}">
                <TextBlock
                    Text="{Binding License, RelativeSource={RelativeSource AncestorType=UserControl}}"
                    FontSize="9"
                    Foreground="#9CA3AF" />
            </Border>
        </Grid>
    </Border>
</UserControl>
