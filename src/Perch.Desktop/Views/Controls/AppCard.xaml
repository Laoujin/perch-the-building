<UserControl
    x:Class="Perch.Desktop.Views.Controls.AppCard"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:controls="clr-namespace:Perch.Desktop.Views.Controls">
    <Border
        x:Name="CardBorder"
        Background="#1A1A2E"
        BorderBrush="#2A2A3E"
        BorderThickness="1"
        CornerRadius="8"
        Padding="12"
        Width="320">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- Row 0: Status ribbon -->
            <controls:StatusRibbon
                Grid.Row="0"
                Status="{Binding Status, RelativeSource={RelativeSource AncestorType=UserControl}}"
                Margin="0,0,0,8" />

            <!-- Row 1: Icon + Name -->
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <ui:SymbolIcon
                    Grid.Column="0"
                    Symbol="Apps24"
                    FontSize="24"
                    Foreground="#888888"
                    VerticalAlignment="Center"
                    Margin="0,0,8,0" />

                <TextBlock
                    Grid.Column="1"
                    Text="{Binding DisplayLabel, RelativeSource={RelativeSource AncestorType=UserControl}}"
                    FontSize="14"
                    FontWeight="SemiBold"
                    Foreground="White"
                    VerticalAlignment="Center"
                    TextTrimming="CharacterEllipsis" />
            </Grid>

            <!-- Row 2: Description -->
            <TextBlock
                Grid.Row="2"
                Text="{Binding Description, RelativeSource={RelativeSource AncestorType=UserControl}}"
                FontSize="12"
                Foreground="#AAAAAA"
                TextWrapping="Wrap"
                TextTrimming="CharacterEllipsis"
                MaxHeight="48"
                Margin="0,6,0,0"
                Visibility="{Binding Description, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource CountToVisibilityConverter}}" />

            <!-- Row 3: Website + GitHub links -->
            <StackPanel
                Grid.Row="3"
                Orientation="Horizontal"
                Margin="0,6,0,0">
                <Button
                    x:Name="WebsiteButton"
                    Background="Transparent"
                    BorderThickness="0"
                    Padding="4,2"
                    Click="OnWebsiteClick"
                    ToolTip="{Binding Website, RelativeSource={RelativeSource AncestorType=UserControl}}"
                    Visibility="{Binding Website, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource CountToVisibilityConverter}}">
                    <StackPanel Orientation="Horizontal">
                        <ui:SymbolIcon Symbol="Globe24" FontSize="12" Foreground="#6B7280" />
                        <TextBlock Text="Website" FontSize="11" Foreground="#6B7280" Margin="4,0,0,0" VerticalAlignment="Center" />
                    </StackPanel>
                </Button>
                <Button
                    x:Name="GitHubButton"
                    Background="Transparent"
                    BorderThickness="0"
                    Padding="4,2"
                    Click="OnGitHubClick"
                    ToolTip="{Binding GitHub, RelativeSource={RelativeSource AncestorType=UserControl}}"
                    Visibility="{Binding GitHub, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource CountToVisibilityConverter}}">
                    <StackPanel Orientation="Horizontal">
                        <ui:SymbolIcon Symbol="Code24" FontSize="12" Foreground="#6B7280" />
                        <TextBlock Text="GitHub" FontSize="11" Foreground="#6B7280" Margin="4,0,0,0" VerticalAlignment="Center" />
                    </StackPanel>
                </Button>
            </StackPanel>

            <!-- Row 4: Action buttons -->
            <StackPanel
                Grid.Row="4"
                Orientation="Horizontal"
                Margin="0,8,0,0">
                <ui:Button
                    x:Name="LinkButton"
                    Content="Link"
                    Appearance="Primary"
                    Click="OnLinkClick"
                    Padding="12,4"
                    FontSize="11"
                    Margin="0,0,6,0"
                    Visibility="{Binding CanLink, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource BooleanToVisibilityConverter}}" />
                <ui:Button
                    x:Name="UnlinkButton"
                    Content="Unlink"
                    Appearance="Secondary"
                    Click="OnUnlinkClick"
                    Padding="12,4"
                    FontSize="11"
                    Margin="0,0,6,0"
                    Visibility="{Binding CanUnlink, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource BooleanToVisibilityConverter}}" />
                <ui:Button
                    x:Name="FixButton"
                    Content="Fix"
                    Appearance="Caution"
                    Click="OnFixClick"
                    Padding="12,4"
                    FontSize="11"
                    Visibility="{Binding CanFix, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource BooleanToVisibilityConverter}}" />
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
